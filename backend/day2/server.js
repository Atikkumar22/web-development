const express = require("express");

const server = express() //server created , or uska instance server constant me agya h 

server.listen(3000) //server ko start krta hai 
console.log("server started on port 3000")